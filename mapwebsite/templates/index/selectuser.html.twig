{% extends 'base.html.twig' %}
{% set nav_off = true %}

{% block title %}
    Select User
{% endblock %}

{% block body %}
    <main>
        <div class="text-center">
            <img src="../images/TUDublinLogo.png" width="350" height="250" class="text-center" alt="logo">
            <h1>I Am A...</h1>

            <div class="row">
                <div class="col">
                    <a href="{{ path('login') }}"><img src="../images/student_icon.png" alt="Student" id="introIcon"></a>
                    <br>
                    <a href="{{ path('login') }}"><button class="btn btn-dark">Student</button></a>
                </div>

                <div class="col">
                    <a href="{{ path('underconstruction') }}"><img src = "../images/visitor_icon.png" alt="Services" id="menuIcon"></a>
                    <br>
                    <a href="{{ path('underconstruction') }}"><button class="btn btn-dark">Guest</button></a>
                </div>
            </div>
        </div>
    </main>
{% endblock %}